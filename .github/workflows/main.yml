name: Keep Alive

on:
  schedule:
    - cron: "*/10 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render backend health endpoint
        run: |
          curl -fsS https://https-dealforage-application-onrender-com.onrender.com/ || echo "Backend not responding"
